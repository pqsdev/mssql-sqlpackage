# mssql-sqlpackage

Docker image for mssql-tools + sqlpackage

## Running

```bash
docker build . -t pqs/mssql-sqlpackage
```

## Building

Clone the repo and run

```bash
docker build . -t pqs/mssql-sqlpackage
```
